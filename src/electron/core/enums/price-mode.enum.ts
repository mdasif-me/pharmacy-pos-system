/**
 * Price mode enums
 */

export enum PriceMode {
  DISCOUNT = 'discount',
  PEAK_HOUR = 'peak-hour',
}

export enum BillMode {
  DISCOUNT = 0,
  PEAK_HOUR = 1,
}

export enum SaleMode {
  DISCOUNT = 0,
  PEAK_HOUR = 1,
}
